# Module 6: Grok Bot + Outcome Swarms

Live unlockable module under src/modules/swarm (`moduleId: swarm`). Final core module of the series.

## Lesson IDs

m6-intro, m6-l1-outcome, m6-v2, m6-l2-assemble, m6-v3, m6-l3-run, m6-v4, m6-l4-teardown, m6-practice-capstone, m6-test, m6-outro

## Flow

outcome → assemble → run loop (S0–S6) → teardown (S7) → capstone → quiz → outro

## Fixture

swarm-van-singel-thu (src/shared/sandboxFixtures/swarmVanSingelThu.ts + swarm-van-singel-thu.json)
- composes prior fixtures (calendar, routines, team, skills)
- orchestration state machine S0–S7 in modules/swarm/flow/orchestration-states.json
- packetGap: signed-contract.pdf; Confirm write before calendar/client sends

## Practices

A outcome (≥2 binary checks), B assemble MVS, C run-loop + Confirm tap, D teardown keep/pause, + capstone (4/5). Quiz 10Q pass ≥8.

## Slack

Thin stub only (status: stub). Grok channels; Slack not required.
